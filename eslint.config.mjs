// ESLint configuration - DISABLED
// All ESLint rules have been turned off to prevent linting errors

const eslintConfig = [
  {
    ignores: ["**/*"], // Ignore all files
  },
];

export default eslintConfig;
